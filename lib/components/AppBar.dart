import 'package:flutter/material.dart';

class AppBarProgesti extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: AppBar(
        title: Text("Notícias"),
        backgroundColor: Colors.deepPurple,
      ),
    );
  }
}
